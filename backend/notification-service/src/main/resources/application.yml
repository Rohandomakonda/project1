server:
  port: 8086

spring:
  application:
    name: notification-service
  cloud:
    loadbalancer:
        ribbon:
          enabled: false
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-group
      auto-offset-reset: earliest

  mail:
    host: smtp.gmail.com
    port: 587
    username: abhiraj.dustakar@gmail.com
    password: amqyybzvskmumkzm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
        prefer-ip-address: true
        hostname: localhost


